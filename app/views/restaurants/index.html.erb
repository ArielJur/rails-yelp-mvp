<ul>
  <% @restaurants.each do |restaurant| %>
    <br><li><%= link_to restaurant.name, restaurant_path(restaurant) %>    <%= link_to 'add review', new_restaurant_review_path(restaurant), class: "btn btn-primary ms-5" %></li>
  <% end %>
</ul>

<%= link_to 'add a new restaurant', new_restaurant_path(@restaurant) %>
